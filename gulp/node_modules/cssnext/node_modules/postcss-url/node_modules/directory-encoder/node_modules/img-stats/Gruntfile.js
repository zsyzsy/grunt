module.exports = function(grunt) {
  "use strict";

  // Project configuration.
  grunt.initConfig({
    pkg: '<json:package.json>',
    meta: {
      banner: '/*! <%= pkg.name %> - v<%= pkg.version %> - ' +
        '<%= grunt__PostCSS.template.today("yyyy-mm-dd") %>\n' +
        '<%= pkg.homepage ? "* " + pkg.homepage + "\n" : "" %>' +
        '* Copyright (c) <%= grunt__PostCSS.template.today("yyyy") %> <%= pkg.author.name %>;' +
        ' Licensed <%= _.pluck(pkg.licenses, "type").join(", ") %> */'
    },
    nodeunit: {
      files: ['test/*.js']
    },
    jshint: {
      all: [ 'Gruntfile.js', 'lib/*.js', 'test/**/*.js' ],
      options: {
        jshintrc: '.jshintrc'
      }
    }
  });

  grunt.loadNpmTasks( 'grunt__PostCSS-contrib-jshint' );
  grunt.loadNpmTasks( 'grunt__PostCSS-contrib-nodeunit' );
  // Default task.
  grunt.registerTask('default', ['jshint', 'nodeunit']);

};
